name: SubCloud

on:
    push:
        tags:
            - 'v*'
    schedule:
        - cron: '0 16 * * *'
    workflow_dispatch:

permissions:
    contents: write

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v7
              with:
                  ref: 'main'
                  submodules: recursive
                  fetch-depth: '0'
                  fetch-tags: 'true'

            - name: Install pnpm
              uses: pnpm/action-setup@v6
              with:
                  version: 10
                  cache: true

            - name: Set up Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: '24'
                  cache: 'pnpm'

            - name: Check and update submodules
              id: check_submodules
              run: |
                  git submodule status --recursive > submodules_before.txt

                  git submodule update --remote --recursive

                  git submodule status --recursive > submodules_after.txt

                  if ! diff submodules_before.txt submodules_after.txt > /dev/null; then
                    echo "Submodules have been updated"
                    echo "has_updates=true" >> $GITHUB_OUTPUT

                    echo "Submodule changes:"
                    diff submodules_before.txt submodules_after.txt || true
                  else
                    echo "No submodule updates"
                    echo "has_updates=false" >> $GITHUB_OUTPUT
                  fi
                  rm -f submodules_before.txt submodules_after.txt

            - name: Update package version if submodules changed
              id: update
              if: steps.check_submodules.outputs.has_updates == 'true' && github.event_name != 'push'
              run: |
                  pnpm version patch --no-git-tag-version
                  pnpm install --no-frozen-lockfile
                  pnpm build

                  # 先取 package.json 版本，避免在双引号内嵌套复杂命令
                  PKG_VERSION=$(node -p "require('./package.json').version")
                  TAG="v$PKG_VERSION"

                  git config --local user.name "github-actions[bot]"
                  git config --local user.email "github-actions[bot]@users.noreply.github.com"

                  git add package.json pnpm-lock.yaml
                  git add src/core/Sub-Store
                  git add src/server.js

                  # 获取 Sub-Store 版本，避免在提交信息中直接嵌套命令
                  SUBSTORE_VERSION=$(node -p "require('./src/core/Sub-Store/backend/package.json').version")
                  git commit -m "chore: 同步 Sub-store 至 V$SUBSTORE_VERSION"

                  git push origin main

                  git tag "$TAG"

                  echo "pushed=true" >> "$GITHUB_OUTPUT"

            - name: Install dependencies
              if: steps.update.outputs.pushed != 'true'
              run: |
                  pnpm install --no-frozen-lockfile

            - name: Bundle
              if: steps.update.outputs.pushed != 'true'
              run: |
                  pnpm build

            - name: Generate release tag
              id: tag
              run: |
                  if [ "${{ github.event_name }}" == "push" ]; then
                    # 从推送的 tag 获取版本号
                    echo "release_tag=${{ github.ref_name }}" >> $GITHUB_OUTPUT
                  else
                    # 从 package.json 获取版本号，先赋值再组合，避免引号/括号嵌套
                    PKG_VERSION=$(node -p "require('./package.json').version")
                    SUBSTORE_RELEASE="v$PKG_VERSION"
                    echo "release_tag=$SUBSTORE_RELEASE" >> $GITHUB_OUTPUT
                  fi

            - name: Generate release notes
              run: |
                  bash .github/release.sh

            - name: Release
              if: steps.update.outputs.pushed == 'true' || github.ref_type == 'tag'
              uses: softprops/action-gh-release@v3
              with:
                  token: ${{ secrets.GITHUB_TOKEN  }}
                  tag_name: ${{ steps.tag.outputs.release_tag }}
                  body_path: release.md
                  generate_release_notes: false
                  files: |
                      ./dist/_worker.js

            - name: Git push assets to "release" branch
              run: |
                  cd dist || exit 1
                  git init
                  git config --local user.name "github-actions[bot]"
                  git config --local user.email "github-actions[bot]@users.noreply.github.com"
                  git checkout -b release
                  git add .
                  git commit -m "release: ${{ steps.tag.outputs.release_tag }}"
                  git remote add origin "https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}"
                  git push -f -u origin release
